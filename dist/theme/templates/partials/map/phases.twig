<div class="phases">
    {% for phase in phases | slice(1) %}
        <span class="phases--phase" data-phase-name="{{phase.name | lower | replace({" ": "-", 'ą':'a', 'ł':'l', 'ś':'s', 'ć':'c'})}}" data-phase="{{phase.slug}}">{{phase.name}}</span>
    {% endfor %}

</div>
